# Copy this file to .streamlit/secrets.toml (gitignored) for local dev.
# On Streamlit Community Cloud, set the same key in the app's Settings ->
# Secrets instead -- never commit a real key (spec section 11).

ANTHROPIC_API_KEY = "sk-ant-..."
